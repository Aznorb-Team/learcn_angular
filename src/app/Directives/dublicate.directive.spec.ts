import { DublicateDirective } from './dublicate.directive';

describe('DublicateDirective', () => {
  it('should create an instance', () => {
    const directive = new DublicateDirective();
    expect(directive).toBeTruthy();
  });
});
