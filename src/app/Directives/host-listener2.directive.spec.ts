import { HostListener2Directive } from './host-listener2.directive';

describe('HostListener2Directive', () => {
  it('should create an instance', () => {
    const directive = new HostListener2Directive();
    expect(directive).toBeTruthy();
  });
});
